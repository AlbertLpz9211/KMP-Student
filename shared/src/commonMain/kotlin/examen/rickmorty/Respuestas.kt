package examen.rickmorty

/**
 * Problema 2 - Nota sobre secretos:
 * Si la API requiriera un secreto (API Key), lo guardaría en el archivo `local.properties` 
 * (el cual está excluido de Git) y lo inyectaría mediante una tarea de Gradle en el código generado (BuildConfig).
 */

/**
 * Problema 5 - Concepto Offline-first:
 * La idea principal es que la base de datos local sea la única fuente de verdad para la UI. 
 * La red solo se utiliza para actualizar la base de datos en segundo plano, lo que garantiza 
 * que la aplicación siempre muestre datos al usuario, incluso sin conexión a internet.
 */
